{
  "type": "function",
  "function": {
    "name": "check_stock",
    "description": "Check if a medication is currently in stock (returns boolean). This is general availability and is not location-specific.",
    "parameters": {
      "type": "object",
      "properties": {
        "med_id": {
          "type": "string",
          "description": "Internal medication id (e.g., MED001). Use resolve_medication_id first if you only have the medication name."
        }
      },
      "required": ["med_id"],
      "additionalProperties": false
    }
  }
}
